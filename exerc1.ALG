#Criar um programa onde o usuário digita dois números e o programa calcula a soma dos mesmos.

a=float(input('Coloque um número: '))
b=float(input('Coloque outro número: '))
c=b+a
print('Sua soma é: ',(c))
